server:
  port: 8060

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: patient-service
          uri: lb://PATIENT-SERVICE
          predicates:
            - Path=/api/patient/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
